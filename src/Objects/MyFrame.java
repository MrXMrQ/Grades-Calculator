package Objects;

import javax.swing.*;

public class MyFrame extends JFrame {
    public MyFrame() {
        setTitle("Grades calculator");
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setSize(500,500);
        setFocusable(true);
    }
}
